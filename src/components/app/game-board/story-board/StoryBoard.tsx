import React from "react";


function StoryBoard({className}: ChildProps): React.JSX.Element {
    return <textarea className={className}> this is the story board</textarea>;
}

export default StoryBoard;